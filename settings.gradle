pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT) // 🔥 MUHIM
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "TowerDefense"
include(":android", ":core")
